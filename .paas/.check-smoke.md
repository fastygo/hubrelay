## Provider smoke (CLI only, no HTTP bot)

Uses `SMOKE_*` environment variables — not the same names as deploy `INPUT_*`.

```bash
export SMOKE_AI_API_KEY="<YOUR_AI_API_KEY>"
export SMOKE_AI_BASE_URL="https://api.example.com/v1"
export SMOKE_AI_MODEL="<YOUR_MODEL_ID>"
export SMOKE_AI_API_MODE="chat_completions"
export SMOKE_PROMPT="Give me a one-line health check reply."

go run ./cmd/provider-smoke
```

## Full bot locally with optional GUI

Same `INPUT_*` names as deploy; handy for parity checks before baking an image.

```bash
export INPUT_AI_API_KEY="<YOUR_AI_API_KEY>"
export INPUT_AI_BASE_URL="https://api.example.com/v1"
export INPUT_AI_MODEL="<YOUR_MODEL_ID>"
export INPUT_AI_API_MODE="chat_completions"
export INPUT_PROXY_SESSION_FORCE="false"
export INPUT_PROXY_SESSION_ENABLED="false"

go run ./cmd/bot
```

Open `http://127.0.0.1:5500` (or the bind address reported by `capabilities`).

### Verify AI is wired

```bash
curl -s http://127.0.0.1:5500/api/command -X POST \
  -H "Content-Type: application/json" \
  -d '{"principal_id":"test","roles":["operator"],"command":"capabilities"}' \
  | grep ai_has_api_key
```

Expected: `"ai_has_api_key":true`

### Quick `ask` via curl

```bash
curl -s http://127.0.0.1:5500/api/command -X POST \
  -H "Content-Type: application/json" \
  -d '{"principal_id":"test","roles":["operator"],"command":"ask","args":{"prompt":"hello"}}'
```

English narrative: [docs/local-testing](../docs/local-testing/README.md).
